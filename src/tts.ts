// Text to Speech page (tts.html): TTS engines from the registry — text box →
// synthesize → audio player + WAV download.
import { initPlayground } from "./pages/playground.js";

initPlayground({ category: "tts" });
