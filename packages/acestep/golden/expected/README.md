# Expected captures

No tensor or audio payload is committed here. Accepted capture manifests will
eventually be small JSON files whose referenced payloads live under the ignored
`golden-local/` directory. Until the native capture harness exists, all four
fixture capture states are intentionally `pending` in `status.json`.
